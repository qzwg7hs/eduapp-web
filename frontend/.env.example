# Copy to .env.local for local overrides, or set as an environment variable
# in your hosting provider's dashboard (e.g. Vercel project settings).
#
# Leave unset for local dev — the Vite dev server proxy handles /api calls.
# In production, set this to your deployed backend's URL:
VITE_API_URL=https://api.your-domain.com/api
